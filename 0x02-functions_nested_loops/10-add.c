#include <stdio.h>
#include "main.h"

/**
 * add -  a function that adds two integers and returns the result.
 * @a: integer one
 * @b: integer two
 * Return: returns sum
 */

int add(int a, int b)
{
	int sum;

	sum = a + b;
	return (sum);
}
